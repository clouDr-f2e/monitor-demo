import routes from './routes'
import { Router, Route, Switch } from 'react-router-dom'

function AppRouter() {
    return (
        <Router>
            <Switch></Switch>
        </Router>
    )
}

export default AppRouter
